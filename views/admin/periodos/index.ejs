<%- include ('../../partials/configure_header.ejs') %> 
<%- include ('../../partials/header_adm.ejs') %>

<body>
  <br />
  <div class="container">
    <hr />
    <h5 class="text-center">PERÍODOS CADASTRADOS</h5>
    <hr />
    <br>
    <!--<a class="btn btn-block btn-info" href="/admin/periodos/novo">NOVO</a>-->
    <br />
    <table class="table text-center rtable mt-3">
      <thead>
        <tr class="ajuste-tr">
          <th>DESCRIÇÃO</th>
          <!--<th>AÇÕES</th>-->
        </tr>
      </thead>
      <tbody>
        <% periodos.forEach(periodo => { %>
        <tr class="ajuste-tr">
          <td class="align-middle" data-title="DESCRIÇÃO:"><%= periodo.descricao %> </td>
          <!--<td class="align-middle">
            <a
              href="/admin/periodos/edicao/<%= periodo.id %>"
              class="btn btn-warning"
              ><i class="bi bi-brush"></i>
            </a>
            <form method="POST" action="/periodos/deletar" style="display: inline" onsubmit="confirmardelete(event,this)">
              <input type="hidden" name="id" value="<%= periodo.id %>">
              <button class="btn btn-danger ajuste_botao_delete">
                <i class="bi bi-trash3"></i>
              </button>
            </form>
          </td>-->
        </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
<%- include ('../../partials/footer.ejs') %>
<%- include ('../../partials/configure_footer.ejs') %>

<script>
  function confirmardelete(event, form) {
    event.preventDefault()
    var decisao = confirm('Deletar permanentemente esse periodo?')
    if (decisao) {
      form.submit()
    }
  }
</script>
